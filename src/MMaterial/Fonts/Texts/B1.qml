import QtQuick

import "../"
import "../../Settings"

BaseText {
    lineHeight: 1.5
    wrapMode: Text.WordWrap

    font {
        family: PublicSans.regular
        pixelSize: Size.pixel16
    }
}
